<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gig.collide.mapper.UserRoleMapper">

    <!-- 基础字段定义 -->
    <sql id="baseColumns">
        id, user_id, role_id, create_time
    </sql>

    <!-- 根据用户ID查询角色列表 -->
    <select id="selectByUserId" resultType="com.gig.collide.domain.UserRole">
        SELECT <include refid="baseColumns"/>
        FROM t_user_role
        WHERE user_id = #{userId}
        ORDER BY create_time ASC
    </select>

    <!-- 根据角色ID查询用户列表 -->
    <select id="selectByRoleId" resultType="com.gig.collide.domain.UserRole">
        SELECT <include refid="baseColumns"/>
        FROM t_user_role
        WHERE role_id = #{roleId}
        ORDER BY create_time ASC
    </select>

    <!-- 检查用户是否拥有指定角色 -->
    <select id="countByUserIdAndRoleId" resultType="int">
        SELECT COUNT(*)
        FROM t_user_role
        WHERE user_id = #{userId} AND role_id = #{roleId}
    </select>

    <!-- 删除用户的所有角色 -->
    <delete id="deleteByUserId">
        DELETE FROM t_user_role
        WHERE user_id = #{userId}
    </delete>

    <!-- 批量插入用户角色关联 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO t_user_role (user_id, role_id, create_time) VALUES
        <foreach collection="userRoles" item="userRole" separator=",">
            (#{userRole.userId}, #{userRole.roleId}, NOW())
        </foreach>
    </insert>

</mapper>
